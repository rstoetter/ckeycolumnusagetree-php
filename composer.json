
{
    "name": "rstoetter/ckeycolumnusagetree-php",
    "description": "The class cKeyColumnUsageTree represents a sorted collection of the key column usage of a mysql database. The main purpose of the class is to determine the dependencies of the tables among each other: The class is able to find dependency paths of more than two tables, when another tables are involved. Dependencies which include self referencing tables are considered, too.",
    "license": "MIT",
    "keywords": ["php","mysql","dependency","tables","dependencies","scanner"],
    "authors": [
        {
            "name": "Rainer Stötter",
            "email": "rstoetter@users.sourceforge.net"
        }
    ],
    "require": {
        "php": ">=7.0",
        "rstoetter/libsqlphp": ">=1.0.0"
    },
    "require-dev": {
        
    },
    "autoload": {
        "psr-4": {
            "rstoetter\\ckeycolumnusagetree-php\\": "src"
        }
    }
}

